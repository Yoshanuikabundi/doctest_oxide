name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - sphinx
  - myst-parser

    # Pip-only installs
  #- pip:
  #  - codecov

